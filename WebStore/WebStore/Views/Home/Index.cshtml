@{
	ViewBag.Title = "Главная страница";
	Layout = "_LayoutBase";
}

@{await Html.RenderPartialAsync("Partial/_Slider"); }

<section>
	<div class="container">
		<div class="row">
			<div class="col-sm-3">
				@{await Html.RenderPartialAsync("Partial/_LeftSideBar"); }
			</div>

			<div class="col-sm-9 padding-right">

				@{await Html.RenderPartialAsync("Partial/_FeaturesItems", new WebStore.ViewModels.ProductViewModel[0]); }

				@{await Html.RenderPartialAsync("Partial/_CategoryTab"); }

				@{await Html.RenderPartialAsync("Partial/_RecommendedItems"); }

			</div>
		</div>
	</div>
</section>

@section Styles{
	<style>
	</style>
}

@section Scripts{
	<script>
		$("section").click(function () {
		});
	</script>
}